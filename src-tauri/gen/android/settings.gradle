pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "dashboard-calc"
include(":app")

// 👇👇👇 هذا هو السطر الذي نسيناه وهو الذي يحل المشكلة 👇👇👇
apply(from = "./tauri.settings.gradle")
