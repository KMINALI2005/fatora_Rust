pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal() // <--- تأكد من وجوده هنا أيضاً
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "dashboard-calc"
include(":app")
