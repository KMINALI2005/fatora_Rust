pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = 'dashboard-calc'
include ':app'

// 👇👇👇 هذا هو السطر السحري المفقود 👇👇👇
include ':tauri-android'
project(':tauri-android').projectDir = new File('./tauri-android')
