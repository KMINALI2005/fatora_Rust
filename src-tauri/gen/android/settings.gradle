pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}

dependencyResolutionManagement {
    // 👇👇👇 التغيير هنا: جعلناها PREFER_SETTINGS بدلاً من FAIL_ON_PROJECT_REPOS 👇👇👇
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "dashboard-calc"
include(":app")
