pluginManagement {
    repositories {
        gradlePluginPortal() // <--- الأولوية الأولى لتحميل إضافات Rust
        google()
        mavenCentral()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "dashboard-calc"
include(":app") // <--- الصيغة الصحيحة في Kotlin (مع الأقواس)

// استدعاء إعدادات Tauri
apply(from = "./tauri.settings.gradle")
