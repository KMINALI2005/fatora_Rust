pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "dashboard-calc"
include ':app'

// التصحيح: استخدام صيغة Groovy الصحيحة (بدون أقواس وبدون علامة يساوي)
apply from: './tauri.settings.gradle'
